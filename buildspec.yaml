version: 0.2

phases:
  install:
    commands:
      - echo No install...
  pre_build:
    commands:
      - echo No pre-build...
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`

artifacts:
  files:
    - '**/*'